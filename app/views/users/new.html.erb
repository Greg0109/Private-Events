<%= form_for(@user) do |f| %>  
  <div class="field"> 
    <%= f.text_field :username, placeholder: "Caps sensitive" %> 
  </div> 
  <div class="actions"> 
    <%= f.submit "Create" %> 
  </div> 
<% end %> 